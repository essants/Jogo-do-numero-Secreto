alert('Bem-vindo ao nosso site!')
let nome = prompt('Digite seu nome:')
let idade = prompt('Digite sua idade:')

if (idade >= 18){
    prompt (nome + ', você é maior de idade, já podemos iniciar o processo para sua habilitação. Boa sorte!')
}
 else {
    prompt (nome + ', você é menor de idade, por isso ainda não podemos iniciar o processo para sua habilitação')
 }